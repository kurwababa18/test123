# Polymarket Terminal Configuration

wallet_address: "0x165Ab23404f12a077c45426a36EDCE442347B477"
refresh_seconds: 15
cache_limit: 200
cache_ttl_markets: 120  # 2 minutes
cache_ttl_feeds: 1800   # 30 minutes

# Optional: Polymarket API credentials for authenticated features
# If not provided, the app will use read-only mode with general market data
polymarket_api_key: null
polymarket_api_secret: null
polymarket_api_passphrase: null

nitter:
  base_urls:
    - "https://nitter.net"
    - "https://nitter.it"
    - "https://nitter.poast.org"
  fallback_on_error: true

# Keyword buckets - add custom searches for each market you care about
# The app will auto-generate buckets from your positions, but you can customize them here
# Format:
#   market_slug: "will-trump-win-2024"
#   keywords:
#     - "keyword OR phrase"
#     - "another keyword"

# Example custom buckets (will be auto-generated from your positions):
custom_keyword_buckets:
  # Example: if you have a position on "Will there be a US-Venezuela conflict?"
  # venezuela_conflict:
  #   keywords:
  #     - "Venezuela AND Pentagon"
  #     - "Trump AND Maduro"
  #     - "Nicolás Maduro OR Maduro"

  # Example: if you have a position on "Government shutdown"
  # government_shutdown:
  #   keywords:
  #     - "government shutdown"
  #     - "continuing resolution"
  #     - "CR vote"

# Legacy topics (for fallback if no positions found)
topics:
  - key: "venezuela_conflict"
    title: "US–Venezuela"
    markets: []
    keywords:
      - "Venezuela AND Pentagon"
      - "Venezuela AND Southern Command"
      - "Trump AND Maduro"
      - "Nicolás Maduro OR Maduro"
      - "Caracas AND (troops OR military OR strike)"

  - key: "gov_shutdown"
    title: "Government Shutdown"
    markets: []
    keywords:
      - "government shutdown"
      - "continuing resolution OR CR vote"
      - "Schumer AND (shutdown OR CR)"
